<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.NontonCuyy" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="colorPrimary">@color/primary</item>
        <item name="colorSecondary">@color/secondary</item>
        <item name="android:windowBackground">?attr/colorSurface</item>

        <!-- Custom attributes for theme switching -->
        <item name="colorSurface">@color/bg_light</item>
        <item name="colorOnSurface">@color/text_light_primary</item>
        <item name="colorOnSurfaceVariant">@color/text_light_secondary</item>
        <item name="colorSurfaceVariant">@color/surface_light</item>
    </style>

    <style name="Theme.NontonCuyy" parent="Base.Theme.NontonCuyy" />

    <style name="RoundedTopCorners" parent="">
        <item name="cornerSizeTopLeft">16dp</item>
        <item name="cornerSizeTopRight">16dp</item>
        <item name="cornerSizeBottomLeft">0dp</item>
        <item name="cornerSizeBottomRight">0dp</item>
    </style>

    <style name="CircleImage" parent="">
        <item name="cornerSize">50%</item>
    </style>
</resources>